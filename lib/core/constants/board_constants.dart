// Default configuration values for board initialization.
// These are only used as initial fallbacks; actual dimensions and palace positions 
// are determined by the TMX map data at runtime.

/// Default width of the grid before map loads.
const int kDefaultBoardWidth = 25;

/// Default height of the grid before map loads.
const int kDefaultBoardHeight = 25;

/// Number of cells from each edge that are unplayable.
const int kPlayableBoundary = 3;
